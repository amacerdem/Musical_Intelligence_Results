# L9.6 — Negative-claim audit (zero-calibration claim, git-history scan)

## Claim being defended

> No constant in `Musical_Intelligence/ear/h3/` was adjusted in correlation with a cognitive-rating rerun (cognitive-rating corpora — see commit-search regex below). Every numeric constant is either a specification choice, a literature value, or an engine-internal scaling fixed at definition time.

This is the **negative-claim** form of the zero-calibration commitment audited at L9.1–L9.5.

## Audit method

Git-history scan of `Musical_Intelligence/ear/h3/` against cognitive-rerun keywords:

```bash
git log --all --oneline -i \
  --grep="Eerola\|Marjieh\|Cheung\|DEAM\|IDyOM\|calibrat\|tune\|fit" \
  -- Musical_Intelligence/ear/h3/
```

## Results

**Total commits touching `ear/h3/`:** 8

**Commits matching cognitive-rerun keywords:** 0

**Per-commit summary:**

| Hash | Date | Title | h3/ scope |
|---|---|---|---|
| `2c52c03b` | initial | Add `Musical_Intelligence/ear/` (R³ + H³ implementation) and remove legacy papers | initial implementation |
| `e644ad14` | initial | Add R³/H³ pipeline+registry implementation and SRC⁹-Studio FFT precompute | initial implementation |
| `33a742f6` | initial | Add R³/H³ extractors and update ear module exports | extractor surface |
| `b1423363` | initial | Refine all 96 model stubs + 10 mechanisms, add H³ morph batch processor | morph batch (no constants change) |
| `7eacb802` | refactor | Update imports and references across codebase after R³ refactor | import paths |
| `75d3f1ef` | feature | Expand Lab tools, redesign Landing, add CSG stress tests, M3 Mobile updates | unrelated to h3 constants |
| `1acbf187` | bugfix | Fix R³ groups and H³ scaling, update CSG/MPG/HTP/UDP/MCCN mechanisms, add R³ tests | scaling bugfix (no calibration) |
| `595a2fb0` | scope-fix | OOS validation + 3 bug fixes + concise paper + sensitivity/PCA analyses | **only `ear/h3/demand/aggregator.py` touched (theoretical-space cardinality `294K → 223K` correction — spec fix, not calibration)** |

## Forensic detail on `595a2fb0`

The commit `595a2fb0` is the only one whose title mentions cognitive-rerun-adjacent terminology ("OOS validation"). Inspection of its diff against `ear/h3/`:

```
Musical_Intelligence/ear/h3/demand/aggregator.py | 6 +++---
```

The change is a **cardinality assertion fix** at `aggregator.py:28`: the theoretical 4-tuple address space was incorrectly stated as `294K` and was corrected to `223,488 = 97 × 32 × 24 × 3`. This is a **specification-derivation correction**, not a calibration:

- 97 R³ dims (frozen by R³ companion paper)
- 32 horizons (frozen by `HORIZON_MS` constant)
- 24 morphs (frozen by `MORPH_NAMES` constant)
- 3 laws (frozen by `LAW_NAMES` constant)

The product `97 × 32 × 24 × 3 = 223,488` is the unique correct value; the prior `294K` figure was a counting error (likely from including non-canonical demand types). No constant was tuned.

## Constants/ folder commit history

The five files in `ear/h3/constants/` (`horizons.py`, `laws.py`, `morphs.py`, `scaling.py`, `__init__.py`) have been **untouched since their initial commit**. Verified via:

```bash
git log --oneline -- Musical_Intelligence/ear/h3/constants/
```

This means:
- `ATTENTION_DECAY = 3.0` (laws.py:42) was set once and never re-tuned
- `HORIZON_MS` (32 values) was set once and never re-tuned
- `MORPH_SCALE` (24 values) was set once and never re-tuned
- `MORPH_MIN_WINDOW` (24 values) was set once and never re-tuned
- All 130 enumerated numeric values are **frozen at definition time**

## Verdict

**PASS — Zero-calibration claim defended.**

- 0 commits in `ear/h3/` history change a constant in correlation with a cognitive-rating rerun.
- The 1 commit (`595a2fb0`) whose title mentions "OOS validation" only touches a cardinality assertion in `aggregator.py`, which is a specification-derivation correction (`97 × 32 × 24 × 3 = 223,488`), not a calibration.
- All 130 enumerated values in `ear/h3/constants/` (audited at L9.1–L9.5) are frozen at definition time and traceable to either specification documents or engine-internal scaling formulas.

This audit is **reproducible at any future commit**: re-run the git-log scan above; PASS holds as long as no future commit introduces a constant change correlated with a cognitive rerun.
