# L9.5 — Causal-law constants provenance

## Constants

```python
N_LAWS: int = 3
LAW_MEMORY:      int = 0    # L0 — past → present (causal backward decay)
LAW_PREDICTION:  int = 1    # L1 — present → future (anticipatory forward)
LAW_INTEGRATION: int = 2    # L2 — past ↔ future (bidirectional symmetric)
LAW_NAMES:       tuple[str, ...] = ("memory", "prediction", "integration")
```

**Locations:**
- [`ear/h3/constants/laws.py:17`](../../../../../Musical_Intelligence/ear/h3/constants/laws.py#L17) — `N_LAWS`
- [`laws.py:22`](../../../../../Musical_Intelligence/ear/h3/constants/laws.py#L22) — `LAW_MEMORY`
- [`laws.py:26`](../../../../../Musical_Intelligence/ear/h3/constants/laws.py#L26) — `LAW_PREDICTION`
- [`laws.py:30`](../../../../../Musical_Intelligence/ear/h3/constants/laws.py#L30) — `LAW_INTEGRATION`
- [`laws.py:37`](../../../../../Musical_Intelligence/ear/h3/constants/laws.py#L37) — `LAW_NAMES`

## Provenance

### N_LAWS = 3
**Specification choice** — three temporal perspectives (memory, prediction, integration). Source: `Docs/H3/H3-TEMPORAL-ARCHITECTURE.md` Section 6.

### Law indices (L0, L1, L2)
**Enumeration of the three causal-law perspectives**, each with documented temporal direction:

| Idx | Name | Direction | Description (per inline docstring) |
|---|---|---|---|
| L0 | memory | past → present | causal exponential decay looking backward from current frame |
| L1 | prediction | present → future | anticipatory exponential projection forward from current frame |
| L2 | integration | past ↔ future | bidirectional symmetric exponential centered on current frame |

The implementation lives in [`ear/h3/attention/`](../../../../../Musical_Intelligence/ear/h3/attention/):
- `attention/memory.py` — applies the kernel backward
- `attention/prediction.py` — applies the kernel forward
- `attention/integration.py` — applies the kernel bidirectionally
- `attention/kernel.py` — shared `exp(-3·(1-p))` kernel reused across all 3 laws

**The three laws share the same kernel constant** (`ATTENTION_DECAY = 3.0`); only the window placement direction differs. This is a **structural commitment**, not a tuned set of parameters.

### LAW_NAMES — string tuple
Human-readable labels for the 3 laws. No numeric content; specification-only.

## Predictive-coding alignment (literature reference, no calibration)

The three-law structure parallels active-inference / predictive-coding theory:
- **L0 memory** ↔ posterior given past evidence (Friston 2005)
- **L1 prediction** ↔ generative forecast over future states (Vuust 2022)
- **L2 integration** ↔ smoothing across temporal context (Rimmele 2018)

These literature anchors are **interpretive citations**, not numerical fits. The law indices and names are arbitrary; the load-bearing content is the structural commitment to three distinct temporal perspectives.

## Classification

| Constant | Class | Provenance |
|---|---|---|
| `N_LAWS = 3` | specification | doc Section 6 |
| `LAW_MEMORY/_PREDICTION/_INTEGRATION` | enumeration | doc Section 6 |
| `LAW_NAMES` | specification | doc Section 6 |

**Zero calibration against human-rated data.** Three-law structure is documented spec; kernel constant `ATTENTION_DECAY = 3.0` is audited at L9.2.

## Verdict

**PASS** — 3+3+3 = 9 enumerated values, all specification-only enumerations. No numeric tuning. Predictive-coding interpretation is a literature anchor, not a fit.
